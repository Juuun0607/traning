<%= form_for [@train, @review], remote: true do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, class: "comment" %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.number_field :rate, value: 1, in: 1...11 %>
  </div>
  <div class="field">
    <%= f.label :review %><br>
    <%= f.text_area :review, class: "comment" %>
  </div>
  <div class="actions">
    <%= f.submit "レビューを投稿する", class: "button_style" %>
  </div>
<% end %>
